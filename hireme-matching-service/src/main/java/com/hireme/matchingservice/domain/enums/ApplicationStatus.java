package com.hireme.matchingservice.domain.enums;

/** Cycle de vie d'une candidature (cf. Dashboard de matching, §8.2). */
public enum ApplicationStatus {
    SUBMITTED,
    VIEWED,
    REJECTED,
    ACCEPTED
}
